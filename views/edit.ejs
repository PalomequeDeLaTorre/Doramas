<!DOCTYPE html>
<html>
<head>
  <title>Editar Dorama</title>
</head>
<style>
    
body {
  font-family: Arial, sans-serif;
  line-height: 1.6;
  text-align: center;
  background-image: url("https://wallpaperaccess.com/full/167261.jpg"); 
  background-size: cover;
  background-repeat: no-repeat;
  background-attachment: fixed;
  background-position: center center;
  background-color: #b40087;
}

h1 {
  margin-top: 40px;
  margin-bottom: 20px;
  padding: 10px;
  background-color: #9ac8e7;
  color: #b40087;
}

form {
  max-width: 400px;
  margin: 20px auto;
  padding: 20px;
  background-color: #fff;
  border: 2px solid #9ac8e7;
}

label {
  display: block;
  margin-bottom: 5px;
}

input[type="text"],
select {
  width: 100%;
  padding: 5px;
  margin-bottom: 10px;
}

input[type="submit"] {
  background-color: #9ac8e7;
  color: #b40087;
  padding: 8px 15px;
  border: none;
  cursor: pointer;
}

input[type="submit"]:hover {
  background-color: #9ac8e7;
}

a {
  display: block;
  margin-top: 10px;
  color: #9ac8e7;
  text-decoration: none;
}

a:hover {
  text-decoration: underline;
}

</style>
<body>
  <h1>Editar Dorama</h1>
  <form method="POST" action="/guardar">
    <input type="hidden" name="id" value="<%= dorama.id %>">
    <label for="name">Nombre:</label>
    <input type="text" id="name" name="name" value="<%= dorama.name %>" required>
    <label for="status">Estado:</label>
    <select id="status" name="status" required>
      <option value="visto" <%= dorama.status === 'visto' ? 'selected' : '' %>>Visto</option>
      <option value="pendiente" <%= dorama.status === 'pendiente' ? 'selected' : '' %>>Pendiente</option>
    </select>
    <input type="submit" value="Guardar">
  </form>
  <a href="/">Volver</a>
</body>
</html>
